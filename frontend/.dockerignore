# node_modules 는 이미지 안에서 npm ci 로 다시 설치합니다.
node_modules/
dist/
.vite/
coverage/
playwright-report/
test-results/
blob-report/
.playwright/
e2e/
design/
design-references/
Dockerfile
.dockerignore
# .env 는 이미지에 넣지 않습니다. 값은 compose 의 빌드 인자로 넘깁니다.
.env
.env.*
!.env.example
*.har
*.webm
